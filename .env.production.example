# Entorno target
ENVIRONMENT=prod

# API endpoints de los microservicios
USERS_BACKEND_URL=https://users-api.example.com
ORDERS_BACKEND_URL=https://orders-api.example.com

# Configuración del backend
BACKEND_PROTOCOL=https

# Configuración del API Gateway
GATEWAY_API_NAME=implementos-gateway
GATEWAY_TITLE="Monorepo API Gateway"
GATEWAY_DESCRIPTION="Gateway principal que unifica todos los microservicios."
GATEWAY_VERSION=1.0.0

# Archivo de salida OpenAPI
OPENAPI_OUTPUT_FILE=openapi-gateway-prod.yaml

# Configuración de Google Cloud para producción 
GCP_PROJECT_ID=mi-proyecto-prod

# Rate limiting (requests per minute)
RATE_LIMIT_PER_MINUTE=100000