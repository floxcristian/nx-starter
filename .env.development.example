# Entorno target
ENVIRONMENT=dev

# API endpoints de los microservicios
USERS_BACKEND_URL=https://users-api.example.com
ORDERS_BACKEND_URL=https://orders-api.example.com

# Configuración del backend
BACKEND_PROTOCOL=http

# Configuración del API Gateway
GATEWAY_API_NAME=mi-empresa-api
GATEWAY_TITLE="Monorepo API Gateway (Development)"
GATEWAY_DESCRIPTION="Gateway de desarrollo que unifica todos los microservicios."
GATEWAY_VERSION=1.0.0-dev

# Archivo de salida OpenAPI
OPENAPI_OUTPUT_FILE=openapi-gateway-dev.yaml

# Configuración de Google Cloud para desarrollo
GCP_PROJECT_ID=mi-proyecto-dev

# Rate limiting (requests per minute)
RATE_LIMIT_PER_MINUTE=10000

